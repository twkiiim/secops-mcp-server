[project]
name = "secops-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.2.4",
    "secops>=0.1.0",
]

[[tool.uv.index]]
url = "https://pypi.org/simple"
default = true
